/* Zoom out animation styles */
main.zoomed-out .main_object {
  top: -20%;
  left: 0;
  transform: scale(0.2) translate(-50%, -50%);
  pointer-events: none;
}

main.zoomed-out .main_object path {
  stroke-width: 2;
}

main.zoomed-out .main_object rect {
  opacity: 0;
}

main.zoomed-out nav {
  transform: translateY(-150%);
  opacity: 0;
}
